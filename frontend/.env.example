# Copy to .env.local and fill in for local dev. For production, set in Vercel.
# Backend API URL (only needed when not using Vite proxy, e.g. production build)
# VITE_API_URL=https://your-backend.railway.app
